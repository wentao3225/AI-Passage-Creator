# APC-4：Phase3 StateGraph 条件分支 — ContentEvaluator + ContentEnhancer

> **目标**：在 Phase3 Graph 中引入条件分支——正文生成后先经过质量评估，不达标的章节自动走增强优化，达标则直接进入配图流程。
>
> **实施日期**：2026-07-03
>
> **依赖**：APC-1（Tool Calling 基础设施）、APC-3（ReAct 循环）——已完成 ✅

---

## 一、改造概述

### 当前 Phase3 Graph（线性链）

```
START → content_generator → image_analyzer → parallel_image_generator → content_merger → END
```

### 目标 Phase3 Graph（带条件分支）

```
START → content_generator
              ↓
       content_evaluator
          ├─ (score ≥ 7) → image_analyzer → parallel_image_generator → content_merger → END
          └─ (score < 7) → content_enhancer → content_evaluator（最多循环 2 次）
```

### 核心逻辑

| 节点 | 职责 |
|------|------|
| `content_evaluator` | 评估正文每章节的分数（1-10），输出通过/不通过 |
| `content_enhancer` | 对不通过的章节做针对性增强优化 |
| `content_evaluator`（第 2 轮） | 二次评估，不通过也放过（兜底），避免死循环 |

### 技术关键点

- **`addConditionalEdges`**：Spring AI Alibaba StateGraph 原生支持的条件边 API
- **兜底机制**：最多 2 轮评估-增强循环，第 2 轮后强制走配图流程
- **状态键**：新增 `contentScore`、`enhancementRound` 两个状态字段

---

## 二、需要修改/新增的文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `ContentEvaluatorAgent.java` | **新建** | 质量评估 Agent，逐章节打分 |
| `ContentEnhancerAgent.java` | **新建** | 内容增强 Agent，用 LLM 针对性优化 |
| `ArticleAgentOrchestrator.java` | **修改** | `buildPhase3Graph()` + KeyStrategyFactory + 注入新 Agent |
| `PromptConstant.java` | **修改** | 新增 Evaluator 和 Enhancer 的 Prompt 模板 |
| `SseMessageTypeEnum.java` | **修改** | 新增 `AGENT_EVALUATING`、`AGENT_ENHANCING` SSE 消息类型 |
| `ArticleAsyncService.java` | **修改** | `buildMessageData()` 中新增 SSE 消息类型处理 |
| `ArticleCreatePage.vue` | **修改** | 前端展示评估/增强状态 |

---

## 三、Step-by-Step 实施步骤

---

### Step 1：新建 `ContentEvaluatorAgent.java`

**路径**：`src/main/java/com/ywt/passage/agent/agents/ContentEvaluatorAgent.java`

```java
package com.ywt.passage.agent.agents;

import com.alibaba.cloud.ai.dashscope.chat.DashScopeChatModel;
import com.alibaba.cloud.ai.graph.OverAllState;
import com.alibaba.cloud.ai.graph.action.NodeAction;
import com.ywt.passage.agent.StreamHandlerContext;
import com.ywt.passage.constant.PromptConstant;
import com.ywt.passage.utils.GsonUtils;
import com.ywt.passage.utils.LlmJsonUtils;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.ai.chat.messages.UserMessage;
import org.springframework.ai.chat.prompt.Prompt;
import org.springframework.stereotype.Component;

import java.util.Map;

/**
 * 内容质量评估 Agent
 * <p>
 * 在正文生成后执行，逐章节评估质量。
 * 输出 { "overallScore": 8, "pass": true, "feedback": "..." }
 * 当 overallScore >= 7 时视为通过。
 */
@Component
@Slf4j
@RequiredArgsConstructor
public class ContentEvaluatorAgent implements NodeAction {

    public static final String INPUT_CONTENT = "content";
    public static final String INPUT_MAIN_TITLE = "mainTitle";
    public static final String INPUT_ENHANCEMENT_ROUND = "enhancementRound";
    public static final String OUTPUT_CONTENT_SCORE = "contentScore";
    public static final String OUTPUT_EVALUATION_FEEDBACK = "evaluationFeedback";

    private final DashScopeChatModel chatModel;

    @Override
    public Map<String, Object> apply(OverAllState state) throws Exception {
        String content = state.value(INPUT_CONTENT)
                .map(Object::toString)
                .orElseThrow(() -> new IllegalArgumentException("缺少正文内容参数"));

        String mainTitle = state.value(INPUT_MAIN_TITLE)
                .map(Object::toString)
                .orElse("");

        // 当前增强轮次，用于兜底判定
        Integer round = state.value(INPUT_ENHANCEMENT_ROUND)
                .map(v -> Integer.parseInt(v.toString()))
                .orElse(0);

        log.info("ContentEvaluatorAgent 开始评估: 正文长度={}, 当前轮次={}", content.length(), round);

        StreamHandlerContext.send("AGENT_EVALUATING:round=" + (round + 1));

        // 如果已经到第二轮（轮次 ≥ 1），直接放过，不再评估
        if (round >= 1) {
            log.info("ContentEvaluatorAgent 兜底：第二轮不评估，直接通过");
            return Map.of(
                    OUTPUT_CONTENT_SCORE, 7,
                    OUTPUT_EVALUATION_FEEDBACK, "第二轮兜底，直接通过"
            );
        }

        // 构建评估 Prompt
        String evalPrompt = buildEvaluationPrompt(mainTitle, content);
        String evalResult = chatModel.call(new Prompt(new UserMessage(evalPrompt)))
                .getResult().getOutput().getText();

        log.info("ContentEvaluatorAgent 评估结果: {}", truncate(evalResult));

        // 解析 LLM 返回的 JSON
        EvaluationResult result = parseEvaluationResult(evalResult);

        log.info("ContentEvaluatorAgent 解析: overallScore={}, pass={}",
                result.overallScore, result.overallScore >= 7);

        return Map.of(
                OUTPUT_CONTENT_SCORE, result.overallScore,
                OUTPUT_EVALUATION_FEEDBACK, result.feedback
        );
    }

    /**
     * 构建评估 Prompt
     */
    private String buildEvaluationPrompt(String mainTitle, String content) {
        return PromptConstant.AGENT_EVALUATOR_PROMPT
                .replace("{mainTitle}", mainTitle)
                .replace("{content}", content);
    }

    /**
     * 解析 LLM 返回的 JSON → EvaluationResult
     */
    private EvaluationResult parseEvaluationResult(String llmOutput) {
        try {
            return GsonUtils.fromJson(
                    LlmJsonUtils.extractJsonCandidate(llmOutput),
                    EvaluationResult.class
            );
        } catch (Exception e) {
            log.warn("ContentEvaluatorAgent 解析失败，默认通过. output={}", truncate(llmOutput));
            return new EvaluationResult(7, "解析失败，默认通过");
        }
    }

    private String truncate(String s) {
        return s == null ? "null" : (s.length() <= 200 ? s : s.substring(0, 200) + "...");
    }

    /**
     * 评估结果内部类
     */
    public static class EvaluationResult {
        public int overallScore;
        public String feedback;

        public EvaluationResult() {}

        public EvaluationResult(int overallScore, String feedback) {
            this.overallScore = overallScore;
            this.feedback = feedback;
        }
    }
}
```

**关键设计决策**：

1. **兜底机制**：`INPUT_ENHANCEMENT_ROUND` 记录增强轮次，第二轮（`round >= 1`）直接 `overallScore = 7` 放过，避免死循环
2. **解析失败兜底**：如果 LLM 返回的不是合法 JSON，`catch` 后默认给 7 分通过
3. **StreamHandlerContext.send()**：向前端推送评估状态，便于可观测性

---

### Step 2：新建 `ContentEnhancerAgent.java`

**路径**：`src/main/java/com/ywt/passage/agent/agents/ContentEnhancerAgent.java`

```java
package com.ywt.passage.agent.agents;

import com.alibaba.cloud.ai.dashscope.chat.DashScopeChatModel;
import com.alibaba.cloud.ai.graph.OverAllState;
import com.alibaba.cloud.ai.graph.action.NodeAction;
import com.ywt.passage.agent.StreamHandlerContext;
import com.ywt.passage.model.enums.SseMessageTypeEnum;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.ai.chat.messages.UserMessage;
import org.springframework.ai.chat.model.ChatResponse;
import org.springframework.ai.chat.prompt.Prompt;
import org.springframework.stereotype.Component;

import java.util.Map;

/**
 * 内容增强 Agent
 * <p>
 * 当 ContentEvaluatorAgent 评分不通过时执行。
 * 使用评估反馈作为优化指引，让 LLM 重新生成更高质的正文。
 */
@Component
@Slf4j
@RequiredArgsConstructor
public class ContentEnhancerAgent implements NodeAction {

    public static final String INPUT_CONTENT = "content";
    public static final String INPUT_MAIN_TITLE = "mainTitle";
    public static final String INPUT_EVALUATION_FEEDBACK = "evaluationFeedback";
    public static final String INPUT_ENHANCEMENT_ROUND = "enhancementRound";
    public static final String OUTPUT_CONTENT = "content";
    public static final String OUTPUT_ENHANCEMENT_ROUND = "enhancementRound";

    private final DashScopeChatModel chatModel;

    @Override
    public Map<String, Object> apply(OverAllState state) throws Exception {
        String content = state.value(INPUT_CONTENT)
                .map(Object::toString)
                .orElseThrow(() -> new IllegalArgumentException("缺少正文内容"));

        String mainTitle = state.value(INPUT_MAIN_TITLE)
                .map(Object::toString)
                .orElse("");

        String feedback = state.value(INPUT_EVALUATION_FEEDBACK)
                .map(Object::toString)
                .orElse("内容需要进一步优化");

        int round = state.value(INPUT_ENHANCEMENT_ROUND)
                .map(v -> Integer.parseInt(v.toString()))
                .orElse(0);

        log.info("ContentEnhancerAgent 开始增强: 正文长度={}, 轮次={}, feedback={}",
                content.length(), round + 1, truncate(feedback));

        StreamHandlerContext.send("AGENT_ENHANCING:round=" + (round + 1));

        String enhancePrompt = buildEnhancePrompt(mainTitle, content, feedback);
        ChatResponse response = chatModel.call(new Prompt(new UserMessage(enhancePrompt)));
        String enhancedContent = response.getResult().getOutput().getText();

        log.info("ContentEnhancerAgent 增强完成: 长度 {} → {}",
                content.length(), enhancedContent != null ? enhancedContent.length() : 0);

        // 更新流式正文到前端
        if (StreamHandlerContext.get() != null) {
            StreamHandlerContext.get()
                    .accept(SseMessageTypeEnum.AGENT3_STREAMING.getStreamingPrefix() + enhancedContent);
        }

        return Map.of(
                OUTPUT_CONTENT, enhancedContent != null ? enhancedContent : content,
                OUTPUT_ENHANCEMENT_ROUND, round + 1
        );
    }

    private String buildEnhancePrompt(String mainTitle, String content, String feedback) {
        return PromptConstant.AGENT_ENHANCER_PROMPT
                .replace("{mainTitle}", mainTitle)
                .replace("{content}", content)
                .replace("{feedback}", feedback);
    }

    private String truncate(String s) {
        return s == null ? "null" : (s.length() <= 200 ? s : s.substring(0, 200) + "...");
    }
}
```

**关键设计决策**：

1. **增量轮次**：输出 `OUTPUT_ENHANCEMENT_ROUND = round + 1`，`OverAllState` 自动将 `enhancementRound` 更新为最新值
2. **流式推送**：增强后的正文也通过 `AGENT3_STREAMING` 推送到前端，前端会自然替换之前的正文
3. **兜底**：如果 LLM 返回 null，保留原始 `content`

---

### Step 3：修改 `ArticleAgentOrchestrator.java`

#### 3a）新增注入两个 Agent

在类的 `@Resource` 注入区域，紧挨着现有 Agent 注入：

```java
@Resource
private ContentMergerAgent contentMergerAgent;
// ▼ 新增 ▼
@Resource
private ContentEvaluatorAgent contentEvaluatorAgent;
@Resource
private ContentEnhancerAgent contentEnhancerAgent;
```

#### 3b）新增状态键常量

在 `KEY_ENABLED_IMAGE_METHODS` 后面新增：

```java
private static final String KEY_ENABLED_IMAGE_METHODS = "enabledImageMethods";
// ▼ 新增 ▼
private static final String KEY_CONTENT_SCORE = "contentScore";
private static final String KEY_EVALUATION_FEEDBACK = "evaluationFeedback";
private static final String KEY_ENHANCEMENT_ROUND = "enhancementRound";
```

#### 3c）新增导入

在文件头部导入区域新增：

```java
import static com.alibaba.cloud.ai.graph.action.AsyncEdgeAction.edge_async;
```

#### 3d）修改 `buildPhase3Graph()` 方法

**完整替换**现有的 `buildPhase3Graph()` 方法：

```java
/**
 * 构建阶段3图：正文生成 → 质量评估（条件分支） → 配图需求分析 → 并行配图生成 → 图文合成
 * <p>
 * 条件分支逻辑：
 * content_evaluator 输出 contentScore
 *   ├─ score ≥ 7 → 通过 → image_analyzer → parallel_image_generator → content_merger → END
 *   └─ score < 7 → 不通过 → content_enhancer → content_evaluator（重新评估，最多 2 轮）
 * 第 2 轮（round ≥ 1）强制通过，避免死循环
 */
private StateGraph buildPhase3Graph() throws GraphStateException {
    KeyStrategyFactory keyStrategyFactory = createKeyStrategyFactory();

    return new StateGraph(keyStrategyFactory)
            // 节点定义
            .addNode("content_generator", node_async(contentGeneratorAgent))
            .addNode("content_evaluator", node_async(contentEvaluatorAgent))
            .addNode("content_enhancer", node_async(contentEnhancerAgent))
            .addNode("image_analyzer", node_async(imageAnalyzerAgent))
            .addNode("parallel_image_generator", node_async(parallelImageGenerator))
            .addNode("content_merger", node_async(contentMergerAgent))
            // 边定义
            .addEdge(START, "content_generator")
            .addEdge("content_generator", "content_evaluator")
            // ★ 条件分支：content_evaluator → image_analyzer 或 content_enhancer
            .addConditionalEdges("content_evaluator",
                    edge_async(state -> {
                        // 从 state 中读取评估分数
                        int score = state.value(KEY_CONTENT_SCORE)
                                .map(v -> Integer.parseInt(v.toString()))
                                .orElse(7); // 默认通过
                        log.info("条件分支路由: contentScore={}, 路由到 {}",
                                score, score >= 7 ? "image_analyzer" : "content_enhancer");
                        return score >= 7 ? "image_analyzer" : "content_enhancer";
                    }),
                    Map.of(
                            "image_analyzer", "image_analyzer",
                            "content_enhancer", "content_enhancer"
                    ))
            // content_enhancer → content_evaluator（循环回去重新评估）
            .addEdge("content_enhancer", "content_evaluator")
            // 通过后的线性链
            .addEdge("image_analyzer", "parallel_image_generator")
            .addEdge("parallel_image_generator", "content_merger")
            .addEdge("content_merger", END);
}
```

#### 3e）修改 `createKeyStrategyFactory()` 方法

在现有 strategies.put 区域最后新增三行：

```java
strategies.put(KEY_ENABLED_IMAGE_METHODS, new ReplaceStrategy());
// ▼ 新增 ▼
strategies.put(KEY_CONTENT_SCORE, new ReplaceStrategy());
strategies.put(KEY_EVALUATION_FEEDBACK, new ReplaceStrategy());
strategies.put(KEY_ENHANCEMENT_ROUND, new ReplaceStrategy());
```

#### 3f）修改 `executePhase3_GenerateContent()`

在 `inputs` map 中新增初始 `enhancementRound`：

```java
Map<String, Object> inputs = new HashMap<>();
inputs.put(KEY_TASK_ID, state.getTaskId());
inputs.put(KEY_MAIN_TITLE, state.getTitle().getMainTitle());
inputs.put(KEY_SUB_TITLE, state.getTitle().getSubTitle());
inputs.put(KEY_OUTLINE, state.getOutline());
inputs.put(KEY_STYLE, state.getStyle());
inputs.put(KEY_ENABLED_IMAGE_METHODS, state.getEnabledImageMethods());
// ▼ 新增 ▼
inputs.put(KEY_ENHANCEMENT_ROUND, 0);  // 初始增强轮次为 0
```

---

### Step 4：修改 `PromptConstant.java`

在文件末尾（最后一个 `*/` 之前）新增两个 Prompt 模板：

```java
  /**
   * 智能体X（Evaluator）：内容质量评估
   * 评估正文的整体质量，给出 1-10 的分数和改进建议
   */
  String AGENT_EVALUATOR_PROMPT = """
      你是一位专业的内容质量评估专家。

      请评估以下文章正文的整体质量：

      文章标题：{mainTitle}

      正文内容：
      {content}

      评估维度（每项 1-10 分，加权计算总分）：
      1. 内容充实度（权重 30%）：章节内容是否足够丰富、300-400 字/章节
      2. 语言自然度（权重 25%）：是否口语化、是否像真人在说话、是否有公文腔
      3. 逻辑连贯性（权重 20%）：章节之间过渡是否自然、逻辑是否通顺
      4. 可读性（权重 15%）：段落长短是否有变化、阅读节奏是否轻快
      5. 场景感（权重 10%）：是否有具体场景、动作、感受和例子

      判定标准：
      - overallScore >= 7：质量合格，通过
      - overallScore < 7：质量不合格，需要增强优化

      请直接返回 JSON 格式，不要有其他内容：
      {
        "overallScore": 8,
        "feedback": "整体质量不错，语言自然流畅。第3章节可以增加更多具体例子来增强场景感。"
      }
      """;

  /**
   * 智能体X（Enhancer）：内容增强优化
   * 根据评估反馈，重新生成/优化正文
   */
  String AGENT_ENHANCER_PROMPT = """
      你是一位资深的内容优化专家，专注提升文章质量。

      请根据以下评估反馈，重新优化文章正文：

      文章标题：{mainTitle}

      当前正文：
      {content}

      评估反馈（需要改进的地方）：
      {feedback}

      优化要求：
      1. 针对评估反馈中提到的具体问题，逐条改进
      2. 如果反馈说"章节内容不够丰富"，则增加 1-2 个具体例子、场景或数据
      3. 如果反馈说"语言有公文腔"，则将正式表达改为口语化、聊天式的表达
      4. 如果反馈说"逻辑不连贯"，则添加过渡句，让章节之间自然衔接
      5. 如果反馈说"缺乏场景感"，则加入具体的动作、感受、画面描写
      6. 保持 Markdown 格式和章节结构不变（## 标题）
      7. 优化后的正文不要出现任何解释、标注或评论，就是纯正文

      请直接返回优化后的完整 Markdown 正文，不要有其他内容。
      """;
```

---

### Step 5：修改 `SseMessageTypeEnum.java`

在现有枚举值中新增两条（紧挨 `AGENT4_COMPLETE` 之前）：

```java
AGENT_EVALUATING("AGENT_EVALUATING", "正在评估内容质量"),
AGENT_ENHANCING("AGENT_ENHANCING", "正在增强内容"),
```

> **注意**：该枚举需要两个参数的构造函数 `(String value, String description)`，注意格式对齐。

---

### Step 6：修改 `ArticleAsyncService.java`

在 `buildMessageData()` 方法中，`AGENT4_ANALYZING` 处理块之后（`return buildCompleteMessageData` 之前）新增两个 if 块：

```java
        // 处理 AGENT_EVALUATING 消息
        if (message.startsWith("AGENT_EVALUATING")) {
            Map<String, Object> data = new HashMap<>();
            data.put("type", "AGENT_EVALUATING");
            int colonIdx = message.indexOf(':');
            if (colonIdx > 0) {
                data.put("content", message.substring(colonIdx + 1));
            }
            return data;
        }

        // 处理 AGENT_ENHANCING 消息
        if (message.startsWith("AGENT_ENHANCING")) {
            Map<String, Object> data = new HashMap<>();
            data.put("type", "AGENT_ENHANCING");
            int colonIdx = message.indexOf(':');
            if (colonIdx > 0) {
                data.put("content", message.substring(colonIdx + 1));
            }
            return data;
        }
```

---

### Step 7：修改前端 `ArticleCreatePage.vue`

#### 7a）在 SSE 消息处理器中新增两个 case

在 `handleSSEMessage` 的 switch 中，`AGENT4_ANALYZING` 之前新增：

```typescript
case 'AGENT_EVALUATING':
    addLog('📊 正在评估内容质量...', 'info')
    break

case 'AGENT_ENHANCING':
    addLog('🔧 发现可优化空间，正在增强内容...', 'info')
    break
```

#### 7b）在创作流程步骤中新增 Evaluator 步骤（可选）

如果需要在前端左侧流程面板中展示评估/增强步骤，在 `agentSteps` 数组中找到合适位置插入：

```typescript
{ title: '内容评估', description: 'AI 评估正文质量' },
{ title: '内容增强', description: '针对薄弱环节优化' },
```

---

### Step 8：编译验证

```bash
# 在项目根目录下执行
mvn compile -DskipTests
```

预期输出：`BUILD SUCCESS`，0 个编译错误。

重点关注：
- `ContentEvaluatorAgent.java` 是否能正确 import `LlmJsonUtils`
- `ContentEnhancerAgent.java` 是否能正确 import `SseMessageTypeEnum`
- `ArticleAgentOrchestrator.java` 中 `addConditionalEdges` 和 `edge_async` API 是否正确

---

### Step 9：运行现有测试

```bash
mvn test -pl . -Dtest="ArticleAgentOrchestratorTest"
```

**注意**：现有 Phase3 测试 mock 了 `contentGeneratorAgent` 的 `apply()`，但现在 Flow 变了（多了 `content_evaluator` 和可能的 `content_enhancer`），测试会失败。

需要在 Phase3 测试类中新增两个 `@Mock`：

```java
@Mock
private ContentEvaluatorAgent contentEvaluatorAgent;

@Mock
private ContentEnhancerAgent contentEnhancerAgent;
```

并 mock 它们的 `apply()` 行为：

```java
// Mock ContentEvaluatorAgent：直接给 8 分通过
doAnswer(invocation -> {
    Map<String, Object> output = new HashMap<>();
    output.put("contentScore", 8);
    output.put("evaluationFeedback", "质量合格");
    return output;
}).when(contentEvaluatorAgent).apply(any());
```

测试期望也要对齐（Phase3 现在多了 `AGENT_EVALUATING` 和 `AGENT_ENHANCING` SSE 消息）。

---

### Step 10：端到端测试

```bash
# 启动后端
mvn spring-boot:run
```

在浏览器中创建一篇文章，观察：

1. **左侧流程面板**：是否出现"内容评估"/"内容增强"步骤
2. **执行日志**：是否出现 `📊 智能体X：正在评估内容质量...`
3. **分支验证**：
   - 如果正文质量较高 → 评估后直接走配图（跳过增强）
   - 如果正文质量较低 → 评估后走增强 → 重新评估 → 配图
   - 增强环节最多执行 1 轮（第二轮 ContentEvaluatorAgent 直接放行）

---

## 四、面试口径

### Q：为什么在 Phase3 引入条件分支？

**A**：正文生成是 LLM 的一次性输出，质量不可控。引入 ContentEvaluator + ContentEnhancer 形成了**自反馈闭环**：
- Evaluator 用 LLM-as-Judge 模式给正文打分（1-10）
- 不达标的章节自动走 Enhancer 针对性优化
- 优化后再次评估，形成"生成→评估→增强→再评估"的质量迭代

这比简单地让一个 Agent 输出正文更可靠，也更符合工程化的质量保障思路。

### Q：为什么不用代码做评估，而是用 LLM？

**A**：文章质量评估是高度主观的——"语言是否自然""是否有场景感""逻辑是否连贯"——这些维度用规则无法量化。LLM-as-Judge 是目前业界（Google、Anthropic）验证过的评估方案。而且我们在打分旁加了兜底机制（解析失败默认通过、第二轮强制放过），确保评估不会成为瓶颈。

### Q：条件分支是用什么技术实现的？

**A**：Spring AI Alibaba StateGraph 原生支持 `addConditionalEdges` API。路由函数 `edge_async(state → "nodeId")` 从 `OverAllState` 中读取 `contentScore` 判断走哪条分支。这不是自己写的 if-else，而是 **Graph 级别的条件边**，类似 LangGraph 的 conditional edge。

### Q：如何避免增强-评估死循环？

**A**：两层兜底：
1. ContentEvaluatorAgent 内部检查 `enhancementRound`，第二轮直接给 7 分通过，不跟 LLM 交互
2. `enhancementRound` 作为 Graph 状态在节点间透传，不会无限递增

---

## 五、改造风险评估

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| 评估不准（误杀好内容） | 🟡 中 | 评估 Prompt 调优 + 兜底默认通过 |
| 增强后反而变差 | 🟡 中 | 第二轮强制放过，不反复折腾 |
| 评估/增强增加总耗时 | 🟢 低 | 只在第一轮做评估，第二轮跳过；增强只有一次 LLM 调用 |
| 现有测试失败 | 🟡 中 | 需要新增 mock 并更新测试期望 |

---

## 六、验收标准

- [ ] `mvn compile -DskipTests` 编译通过，0 错误
- [ ] 端到端跑一遍：INPUT → 标题 → 大纲 → 正文 → **评估** → （增强 → 再评估） → 配图 → 完成
- [ ] 执行日志中出现 `📊 正在评估内容质量...`
- [ ] 如果正文本就不错，评估后直接跳过增强走配图
- [ ] 如果正文质量偏低（Prompt 中可降低要求测试），触发增强流程
- [ ] 增强最多执行 1 轮（第二轮强制放过）
- [ ] 单元测试全部通过
